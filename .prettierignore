# Compiler output (esbuild) — regenerated by every `npm run build`, never hand-formatted.
build/

# Written by tools (release script, i18n→instanceObjects sync, consistency autofixes) in
# expanded-JSON style; hand-formatting it would be undone at the next release.
io-package.json

# Follows the ioBroker bot's single-quote YAML so template PRs from the bot diff clean
# (fleet decision 2026-07-01); the consistency master carries the same format.
.github/dependabot.yml
